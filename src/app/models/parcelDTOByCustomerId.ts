export interface ParcelDTOByCustomerId {
    id: number,
    locationText: string,
    totalHectares: number,
    latitude: number,
    longitude: number,
    createdAt: string,
    isActive: boolean
}
